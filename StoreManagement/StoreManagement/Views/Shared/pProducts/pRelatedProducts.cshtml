@using StoreManagement.Data.GeneralHelper
@model StoreManagement.Data.RequestModel.ProductDetailViewModel
@{
    var relatedProducts = Model.RelatedProducts;
}

@foreach (var e in relatedProducts)
{
     @Html.Partial("DisplayTemplates/ProductThumbnail", e, new ViewDataDictionary(ViewData){
                                {"ProductCategoryName",Model.Category.Name}})
}
